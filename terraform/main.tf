terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = "eu-central-2"
}

module "cloudflare" {
  source = "./modules/cloudflare"
}
